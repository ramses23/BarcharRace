name: Build cartoon logo pack

on:
  push:
    branches:
      - asset-photo-test
    paths:
      - ".github/workflows/build-player-photos.yml"
      - "scripts/build_cartoon_logo_pack_fast.py"
  workflow_dispatch:

permissions:
  contents: write

env:
  PYTHONUNBUFFERED: "1"

jobs:
  build-logos:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out temporary branch
        uses: actions/checkout@v4

      - name: Publish run marker
        run: |
          printf '%s\n' "$GITHUB_RUN_ID" > .cartoon_logo_fast_run_id.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .cartoon_logo_fast_run_id.txt
          git commit -m "chore: record fast cartoon-logo workflow run" || true
          git push origin HEAD:asset-photo-test || true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install image tooling
        run: python -m pip install --disable-pip-version-check Pillow==11.3.0 requests==2.32.4

      - name: Download and normalize cartoon logos
        run: python scripts/build_cartoon_logo_pack_fast.py

      - name: Upload completed pack
        uses: actions/upload-artifact@v4
        with:
          name: cartoon-logo-pack
          path: cartoon_logos_png_max130kb.zip
          if-no-files-found: error
          retention-days: 7
